# Copyright Envoy AI Gateway Authors
# SPDX-License-Identifier: Apache-2.0
# The full text of the Apache license is available in the LICENSE file at
# the root of the repo.

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  labels:
    gateway.networking.k8s.io/policy: direct
  name: guardrailpolicies.aigateway.envoyproxy.io
spec:
  group: aigateway.envoyproxy.io
  names:
    kind: GuardrailPolicy
    listKind: GuardrailPolicyList
    plural: guardrailpolicies
    singular: guardrailpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[-1:].type
      name: Status
      type: string
    deprecated: true
    deprecationWarning: aigateway.envoyproxy.io/v1alpha1 is deprecated; use aigateway.envoyproxy.io/v1beta1
      instead
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GuardrailPolicy evaluates content safety checks for request and
          response payloads.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GuardrailPolicySpec contains the configured checks attached
              to an AIServiceBackend.
            properties:
              maxRequestBodyBytes:
                default: 10485760
                description: MaxRequestBodyBytes is the largest request body evaluated
                  by this policy.
                format: int64
                maximum: 52428800
                minimum: 1024
                type: integer
              maxResponseBodyBytes:
                default: 10485760
                description: MaxResponseBodyBytes is the largest response body buffered
                  and evaluated by this policy.
                format: int64
                maximum: 52428800
                minimum: 1024
                type: integer
              rules:
                description: Rules are executed in order and can evaluate request
                  or response payloads.
                items:
                  description: GuardrailRule defines one content-safety check to apply
                    to a request or response.
                  properties:
                    name:
                      description: Name is a stable identifier for the rule.
                      maxLength: 63
                      minLength: 1
                      type: string
                    phase:
                      description: Phase determines whether the rule runs against
                        the request or the response payload.
                      enum:
                      - Request
                      - Response
                      type: string
                    provider:
                      description: Provider configures how the rule is evaluated.
                      properties:
                        action:
                          default: Block
                          description: Action is the action taken when the rule is
                            matched.
                          enum:
                          - Block
                          - Monitor
                          - Mask
                          type: string
                        azureContentSafety:
                          description: AzureContentSafety configures Azure AI Content
                            Safety.
                          properties:
                            apiKeySecretRef:
                              description: APIKeySecretRef references a Secret whose
                                apiKey entry is sent to Azure.
                              properties:
                                group:
                                  default: ""
                                  description: |-
                                    Group is the group of the referent. For example, "gateway.networking.k8s.io".
                                    When unspecified or empty string, core API group is inferred.
                                  maxLength: 253
                                  pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                  type: string
                                kind:
                                  default: Secret
                                  description: Kind is kind of the referent. For example
                                    "Secret".
                                  maxLength: 63
                                  minLength: 1
                                  pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                                  type: string
                                name:
                                  description: Name is the name of the referent.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: |-
                                    Namespace is the namespace of the referenced object. When unspecified, the local
                                    namespace is inferred.

                                    Note that when a namespace different than the local namespace is specified,
                                    a ReferenceGrant object is required in the referent namespace to allow that
                                    namespace's owner to accept the reference. See the ReferenceGrant
                                    documentation for details.

                                    Support: Core
                                  maxLength: 63
                                  minLength: 1
                                  pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                                  type: string
                              required:
                              - name
                              type: object
                            apiVersion:
                              default: "2024-09-01"
                              type: string
                            endpoint:
                              format: uri
                              type: string
                            severityThreshold:
                              format: int32
                              maximum: 7
                              minimum: 0
                              type: integer
                          required:
                          - apiKeySecretRef
                          - endpoint
                          type: object
                        bedrock:
                          description: Bedrock configures AWS Bedrock Guardrails.
                          properties:
                            credentialsSecretRef:
                              description: CredentialsSecretRef optionally references
                                a Secret whose credentials entry contains an AWS shared
                                credentials file.
                              properties:
                                group:
                                  default: ""
                                  description: |-
                                    Group is the group of the referent. For example, "gateway.networking.k8s.io".
                                    When unspecified or empty string, core API group is inferred.
                                  maxLength: 253
                                  pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                  type: string
                                kind:
                                  default: Secret
                                  description: Kind is kind of the referent. For example
                                    "Secret".
                                  maxLength: 63
                                  minLength: 1
                                  pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                                  type: string
                                name:
                                  description: Name is the name of the referent.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: |-
                                    Namespace is the namespace of the referenced object. When unspecified, the local
                                    namespace is inferred.

                                    Note that when a namespace different than the local namespace is specified,
                                    a ReferenceGrant object is required in the referent namespace to allow that
                                    namespace's owner to accept the reference. See the ReferenceGrant
                                    documentation for details.

                                    Support: Core
                                  maxLength: 63
                                  minLength: 1
                                  pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                                  type: string
                              required:
                              - name
                              type: object
                            endpoint:
                              description: Endpoint overrides the Bedrock runtime
                                endpoint, primarily for private endpoints and testing.
                              format: uri
                              type: string
                            guardrailIdentifier:
                              minLength: 1
                              type: string
                            guardrailVersion:
                              minLength: 1
                              type: string
                            region:
                              minLength: 1
                              type: string
                          required:
                          - guardrailIdentifier
                          - guardrailVersion
                          - region
                          type: object
                        failureMode:
                          default: FailClosed
                          description: FailureMode determines whether provider errors
                            block or allow the request.
                          enum:
                          - FailClosed
                          - FailOpen
                          type: string
                        maskReplacement:
                          default: '[REDACTED]'
                          description: MaskReplacement is used by Regex and Presidio
                            Mask actions.
                          type: string
                        message:
                          description: Message is returned to the caller when the
                            rule blocks a request or response.
                          type: string
                        pattern:
                          description: Pattern is used for deterministic regex-based
                            evaluations.
                          type: string
                        presidio:
                          description: Presidio configures the Presidio analyzer provider.
                          properties:
                            apiKeySecretRef:
                              description: APIKeySecretRef optionally references a
                                Secret whose apiKey entry is sent as a Bearer token.
                              properties:
                                group:
                                  default: ""
                                  description: |-
                                    Group is the group of the referent. For example, "gateway.networking.k8s.io".
                                    When unspecified or empty string, core API group is inferred.
                                  maxLength: 253
                                  pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                  type: string
                                kind:
                                  default: Secret
                                  description: Kind is kind of the referent. For example
                                    "Secret".
                                  maxLength: 63
                                  minLength: 1
                                  pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                                  type: string
                                name:
                                  description: Name is the name of the referent.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: |-
                                    Namespace is the namespace of the referenced object. When unspecified, the local
                                    namespace is inferred.

                                    Note that when a namespace different than the local namespace is specified,
                                    a ReferenceGrant object is required in the referent namespace to allow that
                                    namespace's owner to accept the reference. See the ReferenceGrant
                                    documentation for details.

                                    Support: Core
                                  maxLength: 63
                                  minLength: 1
                                  pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                                  type: string
                              required:
                              - name
                              type: object
                            endpoint:
                              format: uri
                              type: string
                            language:
                              default: en
                              type: string
                            scoreThresholdPercent:
                              format: int32
                              maximum: 100
                              minimum: 0
                              type: integer
                          required:
                          - endpoint
                          type: object
                        timeoutSeconds:
                          default: 10
                          description: TimeoutSeconds limits each external provider
                            evaluation.
                          format: int32
                          maximum: 60
                          minimum: 1
                          type: integer
                        type:
                          description: Type identifies the guardrail implementation.
                          enum:
                          - Regex
                          - Presidio
                          - Bedrock
                          - AzureContentSafety
                          type: string
                      required:
                      - type
                      type: object
                      x-kubernetes-validations:
                      - message: Regex requires pattern and no external provider configuration
                        rule: self.type != 'Regex' || (has(self.pattern) && self.pattern
                          != ” && !has(self.presidio) && !has(self.bedrock) && !has(self.azureContentSafety))
                      - message: Presidio requires only presidio provider configuration
                        rule: self.type != 'Presidio' || (!has(self.pattern) && has(self.presidio)
                          && !has(self.bedrock) && !has(self.azureContentSafety))
                      - message: Bedrock requires only bedrock provider configuration
                        rule: self.type != 'Bedrock' || (!has(self.pattern) && has(self.bedrock)
                          && !has(self.presidio) && !has(self.azureContentSafety))
                      - message: AzureContentSafety requires only azureContentSafety
                          provider configuration
                        rule: self.type != 'AzureContentSafety' || (!has(self.pattern)
                          && has(self.azureContentSafety) && !has(self.presidio) &&
                          !has(self.bedrock))
                      - message: AzureContentSafety does not support Mask
                        rule: self.action != 'Mask' || self.type != 'AzureContentSafety'
                  required:
                  - name
                  - phase
                  - provider
                  type: object
                maxItems: 32
                minItems: 1
                type: array
                x-kubernetes-validations:
                - message: rule name must be unique within the policy
                  rule: self.all(rule, self.exists_one(other, other.name == rule.name))
              targetRefs:
                description: TargetRefs identify the AIServiceBackend resources this
                  GuardrailPolicy is attached to.
                items:
                  description: |-
                    LocalPolicyTargetReference identifies an API object to apply a direct or
                    inherited policy to. This should be used as part of Policy resources
                    that can target Gateway API resources. For more information on how this
                    policy attachment model works, and a sample Policy resource, refer to
                    the policy attachment documentation for Gateway API.
                  properties:
                    group:
                      description: Group is the group of the target resource.
                      maxLength: 253
                      pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    kind:
                      description: Kind is kind of the target resource.
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                      type: string
                    name:
                      description: Name is the name of the target resource.
                      maxLength: 253
                      minLength: 1
                      type: string
                  required:
                  - group
                  - kind
                  - name
                  type: object
                maxItems: 16
                minItems: 1
                type: array
                x-kubernetes-validations:
                - message: targetRefs must reference AIServiceBackend resources
                  rule: self.all(ref, ref.group == 'aigateway.envoyproxy.io' && ref.kind
                    == 'AIServiceBackend')
            type: object
          status:
            description: Status defines the status details of the GuardrailPolicy.
            properties:
              conditions:
                description: |-
                  Conditions is the list of conditions by the reconciliation result.
                  Currently, at most one condition is set.

                  Known .status.conditions.type are: "Accepted", "NotAccepted".
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[-1:].type
      name: Status
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: GuardrailPolicy evaluates content safety checks for request and
          response payloads.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GuardrailPolicySpec contains the configured checks attached
              to an AIServiceBackend.
            properties:
              maxRequestBodyBytes:
                default: 10485760
                description: MaxRequestBodyBytes is the largest request body evaluated
                  by this policy.
                format: int64
                maximum: 52428800
                minimum: 1024
                type: integer
              maxResponseBodyBytes:
                default: 10485760
                description: MaxResponseBodyBytes is the largest response body buffered
                  and evaluated by this policy.
                format: int64
                maximum: 52428800
                minimum: 1024
                type: integer
              rules:
                description: Rules are executed in order and can evaluate request
                  or response payloads.
                items:
                  description: GuardrailRule defines one content-safety check to apply
                    to a request or response.
                  properties:
                    name:
                      description: Name is a stable identifier for the rule.
                      maxLength: 63
                      minLength: 1
                      type: string
                    phase:
                      description: Phase determines whether the rule runs against
                        the request or the response payload.
                      enum:
                      - Request
                      - Response
                      type: string
                    provider:
                      description: Provider configures how the rule is evaluated.
                      properties:
                        action:
                          default: Block
                          description: Action is the action taken when the rule is
                            matched.
                          enum:
                          - Block
                          - Monitor
                          - Mask
                          type: string
                        azureContentSafety:
                          description: AzureContentSafety configures Azure AI Content
                            Safety.
                          properties:
                            apiKeySecretRef:
                              description: APIKeySecretRef references a Secret whose
                                apiKey entry is sent to Azure.
                              properties:
                                group:
                                  default: ""
                                  description: |-
                                    Group is the group of the referent. For example, "gateway.networking.k8s.io".
                                    When unspecified or empty string, core API group is inferred.
                                  maxLength: 253
                                  pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                  type: string
                                kind:
                                  default: Secret
                                  description: Kind is kind of the referent. For example
                                    "Secret".
                                  maxLength: 63
                                  minLength: 1
                                  pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                                  type: string
                                name:
                                  description: Name is the name of the referent.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: |-
                                    Namespace is the namespace of the referenced object. When unspecified, the local
                                    namespace is inferred.

                                    Note that when a namespace different than the local namespace is specified,
                                    a ReferenceGrant object is required in the referent namespace to allow that
                                    namespace's owner to accept the reference. See the ReferenceGrant
                                    documentation for details.

                                    Support: Core
                                  maxLength: 63
                                  minLength: 1
                                  pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                                  type: string
                              required:
                              - name
                              type: object
                            apiVersion:
                              default: "2024-09-01"
                              type: string
                            endpoint:
                              format: uri
                              type: string
                            severityThreshold:
                              format: int32
                              maximum: 7
                              minimum: 0
                              type: integer
                          required:
                          - apiKeySecretRef
                          - endpoint
                          type: object
                        bedrock:
                          description: Bedrock configures AWS Bedrock Guardrails.
                          properties:
                            credentialsSecretRef:
                              description: CredentialsSecretRef optionally references
                                a Secret whose credentials entry contains an AWS shared
                                credentials file.
                              properties:
                                group:
                                  default: ""
                                  description: |-
                                    Group is the group of the referent. For example, "gateway.networking.k8s.io".
                                    When unspecified or empty string, core API group is inferred.
                                  maxLength: 253
                                  pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                  type: string
                                kind:
                                  default: Secret
                                  description: Kind is kind of the referent. For example
                                    "Secret".
                                  maxLength: 63
                                  minLength: 1
                                  pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                                  type: string
                                name:
                                  description: Name is the name of the referent.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: |-
                                    Namespace is the namespace of the referenced object. When unspecified, the local
                                    namespace is inferred.

                                    Note that when a namespace different than the local namespace is specified,
                                    a ReferenceGrant object is required in the referent namespace to allow that
                                    namespace's owner to accept the reference. See the ReferenceGrant
                                    documentation for details.

                                    Support: Core
                                  maxLength: 63
                                  minLength: 1
                                  pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                                  type: string
                              required:
                              - name
                              type: object
                            endpoint:
                              description: Endpoint overrides the Bedrock runtime
                                endpoint, primarily for private endpoints and testing.
                              format: uri
                              type: string
                            guardrailIdentifier:
                              minLength: 1
                              type: string
                            guardrailVersion:
                              minLength: 1
                              type: string
                            region:
                              minLength: 1
                              type: string
                          required:
                          - guardrailIdentifier
                          - guardrailVersion
                          - region
                          type: object
                        failureMode:
                          default: FailClosed
                          description: FailureMode determines whether provider errors
                            block or allow the request.
                          enum:
                          - FailClosed
                          - FailOpen
                          type: string
                        maskReplacement:
                          default: '[REDACTED]'
                          description: MaskReplacement is used by Regex and Presidio
                            Mask actions.
                          type: string
                        message:
                          description: Message is returned to the caller when the
                            rule blocks a request or response.
                          type: string
                        pattern:
                          description: Pattern is used for deterministic regex-based
                            evaluations.
                          type: string
                        presidio:
                          description: Presidio configures the Presidio analyzer provider.
                          properties:
                            apiKeySecretRef:
                              description: APIKeySecretRef optionally references a
                                Secret whose apiKey entry is sent as a Bearer token.
                              properties:
                                group:
                                  default: ""
                                  description: |-
                                    Group is the group of the referent. For example, "gateway.networking.k8s.io".
                                    When unspecified or empty string, core API group is inferred.
                                  maxLength: 253
                                  pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                  type: string
                                kind:
                                  default: Secret
                                  description: Kind is kind of the referent. For example
                                    "Secret".
                                  maxLength: 63
                                  minLength: 1
                                  pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                                  type: string
                                name:
                                  description: Name is the name of the referent.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: |-
                                    Namespace is the namespace of the referenced object. When unspecified, the local
                                    namespace is inferred.

                                    Note that when a namespace different than the local namespace is specified,
                                    a ReferenceGrant object is required in the referent namespace to allow that
                                    namespace's owner to accept the reference. See the ReferenceGrant
                                    documentation for details.

                                    Support: Core
                                  maxLength: 63
                                  minLength: 1
                                  pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                                  type: string
                              required:
                              - name
                              type: object
                            endpoint:
                              format: uri
                              type: string
                            language:
                              default: en
                              type: string
                            scoreThresholdPercent:
                              format: int32
                              maximum: 100
                              minimum: 0
                              type: integer
                          required:
                          - endpoint
                          type: object
                        timeoutSeconds:
                          default: 10
                          description: TimeoutSeconds limits each external provider
                            evaluation.
                          format: int32
                          maximum: 60
                          minimum: 1
                          type: integer
                        type:
                          description: Type identifies the guardrail implementation.
                          enum:
                          - Regex
                          - Presidio
                          - Bedrock
                          - AzureContentSafety
                          type: string
                      required:
                      - type
                      type: object
                      x-kubernetes-validations:
                      - message: Regex requires pattern and no external provider configuration
                        rule: self.type != 'Regex' || (has(self.pattern) && self.pattern
                          != ” && !has(self.presidio) && !has(self.bedrock) && !has(self.azureContentSafety))
                      - message: Presidio requires only presidio provider configuration
                        rule: self.type != 'Presidio' || (!has(self.pattern) && has(self.presidio)
                          && !has(self.bedrock) && !has(self.azureContentSafety))
                      - message: Bedrock requires only bedrock provider configuration
                        rule: self.type != 'Bedrock' || (!has(self.pattern) && has(self.bedrock)
                          && !has(self.presidio) && !has(self.azureContentSafety))
                      - message: AzureContentSafety requires only azureContentSafety
                          provider configuration
                        rule: self.type != 'AzureContentSafety' || (!has(self.pattern)
                          && has(self.azureContentSafety) && !has(self.presidio) &&
                          !has(self.bedrock))
                      - message: AzureContentSafety does not support Mask
                        rule: self.action != 'Mask' || self.type != 'AzureContentSafety'
                  required:
                  - name
                  - phase
                  - provider
                  type: object
                maxItems: 32
                minItems: 1
                type: array
                x-kubernetes-validations:
                - message: rule name must be unique within the policy
                  rule: self.all(rule, self.exists_one(other, other.name == rule.name))
              targetRefs:
                description: TargetRefs identify the AIServiceBackend resources this
                  GuardrailPolicy is attached to.
                items:
                  description: |-
                    LocalPolicyTargetReference identifies an API object to apply a direct or
                    inherited policy to. This should be used as part of Policy resources
                    that can target Gateway API resources. For more information on how this
                    policy attachment model works, and a sample Policy resource, refer to
                    the policy attachment documentation for Gateway API.
                  properties:
                    group:
                      description: Group is the group of the target resource.
                      maxLength: 253
                      pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    kind:
                      description: Kind is kind of the target resource.
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                      type: string
                    name:
                      description: Name is the name of the target resource.
                      maxLength: 253
                      minLength: 1
                      type: string
                  required:
                  - group
                  - kind
                  - name
                  type: object
                maxItems: 16
                minItems: 1
                type: array
                x-kubernetes-validations:
                - message: targetRefs must reference AIServiceBackend resources
                  rule: self.all(ref, ref.group == 'aigateway.envoyproxy.io' && ref.kind
                    == 'AIServiceBackend')
            type: object
          status:
            description: Status defines the status details of the GuardrailPolicy.
            properties:
              conditions:
                description: |-
                  Conditions is the list of conditions by the reconciliation result.
                  Currently, at most one condition is set.

                  Known .status.conditions.type are: "Accepted", "NotAccepted".
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
